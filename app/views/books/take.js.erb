$(".history-list").html("<%= j (render 'historys/histories', histories: @histories) %>")
$(".take-return").html('<%= j (button_to "Return", return_book_path(@book.id), id: 'take-return', class: "btn btn-outline-warning far", type: "button", method: "put", remote: true) %>')
